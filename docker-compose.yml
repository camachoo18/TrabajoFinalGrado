services:
  agenda-contactos:
    build: .
    container_name: agenda-contactos-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm start
